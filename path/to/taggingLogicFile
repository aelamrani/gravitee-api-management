     // Existing tagging logic
    if (isDefaultGateway() && environmentSupportsTagging()) {
        applyEnvironmentSpecificTag();
    } else if (isDefaultGateway()) {
        applyDefaultTag();
     }
